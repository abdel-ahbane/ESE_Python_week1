
if __name__ == "__main__":
    zahl = 10
    kommazahl = 10.5
    text = "hello, world!"
    wahrheitswert = True

    print("zahl is an " + str(type(zahl)) + " type with value: " + str(zahl))
    print("kommazahl is an " + str(type(kommazahl)) + " type with value: " + str(kommazahl))
    print("text is an " + str(type(text)) + " type with value: " + text)
    print("wahrheitswert is an " + str(type(wahrheitswert)) + " type with value: " + str(wahrheitswert))
